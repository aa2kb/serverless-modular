var {feature}Model = require('./{feature}-model.js');

function {feature}Controller(){
  // Constructor
}

{feature}Controller.prototype.sendTestResponse = () => new Promise((resolve, reject) => {
  resolve({
    statusCode: 200,
    headers: {
      'Access-Control-Allow-Origin': '*'
    },
    body: JSON.stringify({
      message: 'Running {feature} feature test handler. SMM is AWESOME!'
    }),
  });
});


module.exports = {feature}Controller;