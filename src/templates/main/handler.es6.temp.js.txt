import {featureInitCap} from './{feature}-controller.js';
const {feature}Ctrl = new {featureInitCap}();

export async function test(event, context, callback) {
  const response = await {feature}Ctrl.sendTestResponse();

  return response;
};

