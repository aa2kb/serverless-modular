const {feature}Model = require('./{feature}-model.js');

export default class {feature}Controller {
  sendTestResponse = () => new Promise((resolve, reject) => {
    resolve({
      statusCode: 200,
      headers: {
        'Access-Control-Allow-Origin': '*'
      },
      body: JSON.stringify({
        message: 'Running {feature} feature test handler. SMM is AWESOME!'
      }),
    });
  });
}
